fn main() {
    // A normal array
    let nums: [i32; 4] = [10, 20, 30, 40];

    // ▲ Index zero (must be > 0)
    let x0 = nums[0];

    // ▲ Negative index (must be > 0)
    let xneg = nums[-2];

    println!("x0 = {x0}, xneg = {xneg}", x0=x0, xneg=xneg);
}
